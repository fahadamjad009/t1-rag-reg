# Fail-closed threshold:
# If the best retrieved chunk score is below this, the system refuses to answer.
RAG_MIN_SCORE=0.25

# Chunking
CHUNK_MAX_WORDS=220
CHUNK_OVERLAP_WORDS=40

# Default retrieval size
TOP_K=5

# Data root (persisted on disk)
DATA_DIR=data
